# State - sector-control/undeclared

THIS FIXTURE EXISTS TO FAIL.

A managed build whose site says "physiotherapy" in its own <h1> and whose
facts.md never says what trade the business is in. The point of the finding is
not that the site is wrong - it is that nobody was asked, so a whole regulated
regime is sitting unchecked behind a question the interview skipped.

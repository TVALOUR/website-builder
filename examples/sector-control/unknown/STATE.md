# State - sector-control/unknown

THIS FIXTURE EXISTS TO FAIL.

A managed build that declares a sector id with no file behind it. A typo in this
row silently turns every trade duty off, which is precisely the failure mode
worth engineering against - so it is a blocker rather than a shrug.

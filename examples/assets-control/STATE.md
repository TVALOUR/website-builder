# State — assets-control fixture

**Build:** assets-control

## Next action
Nothing. This folder exists so the checker treats it as a build this repo produced,
which is what switches the assets and motion policies from observations into blockers.
See assets/MANIFEST.md — it is deliberately wrong.

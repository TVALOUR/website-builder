# Facts — Bare Control

This file is deliberately NOT a table. It is prose, the way somebody writes when
they have understood that a ledger is wanted and not that it has to be
structured enough to check against.

The phone number is 01392 000 111 and a full set is £250.

`facts/ledger-unstructured` exists for exactly this file: a checker that accepts
this as a ledger is doing substring search over prose, which is what the first
version of the facts gate did and why it was rebuilt.

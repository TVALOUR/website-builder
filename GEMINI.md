# Website-Builder — Gemini CLI entry

Read **`AGENTS.md`** now, before anything else — it is your full operating
contract for this workspace (identity, the six-stage pipeline, checkpoint rhythm,
hard rules). Follow it exactly. Then read `SESSION.md` to pick up any in-progress
build.

Pick your run mode by the capability probe in `AGENTS.md` § Run modes — by what
your harness can actually do (sub-agents? browser? web fetch? image generation?),
not by assumption. Never claim a capability you did not exercise.
